{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "aptPackages": ["ffmpeg"]
  },
  "deploy": {
    "startCommand": "gunicorn -w 4 -b 0.0.0.0:$PORT app:app --timeout 300",
    "restartPolicyType": "ON_FAILURE"
  }
}
